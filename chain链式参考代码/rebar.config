{erl_opts, [debug_info, {parse_transform, lager_transform}]}.
{deps, [{lager, ".*", {git, "git://github.com/basho/lager.git", {tag, "3.1.0"}}}]}.

% rebar.config：Rebar 的配置文件，指定了 Erlang 编译选项（如调试信息和解析转换）以及项目的依赖（如 lager 日志库）。